# evitaremos usar array puro, se precisamos de trabalho numérico temos costume de usar o numpy

import numpy as np
import array as arr
arr.array('d', [1, 3.5])

numeros = np.array([1, 3.5])
print(numeros)
